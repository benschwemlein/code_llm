[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short -m "not chunking_eval"
markers =
    chunking_eval: chunking strategy comparison (run separately with -m chunking_eval)
